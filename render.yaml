services:
  - type: web
    name: shorties-server
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn shorties_server:app
    envVars:
      - key: PORT
        value: 10000